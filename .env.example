# Firebase web app config — same project as the main Scholarton app, since the
# admin authenticates against the same Firebase Auth user pool.
VITE_API_URL=
VITE_API_KEY=
VITE_AUTH_DOMAIN=
VITE_PROJECT_ID=
VITE_STORAGE_BUCKET=
VITE_MESSAGING_SENDER_ID=
VITE_APP_ID=
